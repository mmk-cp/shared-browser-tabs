services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data
      - ./browser-data:/browser-data
    restart: unless-stopped
    shm_size: "1gb"
